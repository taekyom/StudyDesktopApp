<Window x:Class="WpfLoginApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLoginApp"
        mc:Ignorable="d"
        Title="Login" Height="320" Width="320" Background="LightSteelBlue" Initialized="Window_Initialized">
    <Grid Margin="20" Background="White">
        <StackPanel Margin="20">
            <Label Content="Login" FontSize="20" HorizontalAlignment="Center"/>
            <Separator Background="Orange"/>
            <Label Content="UserId" FontSize="16"/>
            <TextBox x:Name="TxtUserName" FontSize="14" Height="25" KeyDown="TxtUserName_KeyDown"/>
            <Label Content="Password" FontSize="16"/>
            <PasswordBox x:Name="TxtUserPwd" FontSize="14" Height="25" KeyDown="TxtUserPwd_KeyDown"/>
            <Button x:Name="BtnLogin" Content="로그인" FontSize="16" Width="100" Height="30" Margin="15"/>
        </StackPanel>
    </Grid>
</Window>
